{
  "schema": 2,
  "status": "In Development",
  "workflow": "direct-merge",
  "execution_mode": "subagents",
  "goal": "A1 owns S1 but its files.create lists the wrong spec path.",
  "architecture": "Two phases. The deterministic path for A1 should be e2e/a1.spec.ts but the plan declares e2e/foo.spec.ts.",
  "tech_stack": "bash, jq, playwright",
  "phases": [
    {
      "letter": "A",
      "name": "Foundation",
      "status": "Not Started",
      "depends_on": [],
      "rationale": "Render markdown scenario lives here.",
      "tasks": [
        {
          "id": "A1",
          "name": "Render markdown",
          "status": "pending",
          "depends_on": [],
          "files": {
            "create": ["e2e/foo.spec.ts"],
            "modify": [],
            "test": []
          },
          "verification": "npx playwright test --grep S1",
          "done_when": "S1 passes",
          "e2e_scenarios": ["S1"]
        }
      ]
    },
    {
      "letter": "B",
      "name": "Comments",
      "status": "Not Started",
      "depends_on": ["A"],
      "rationale": "Comment scenario lives here.",
      "tasks": [
        {
          "id": "B1",
          "name": "Add comment",
          "status": "pending",
          "depends_on": ["A1"],
          "files": {
            "create": ["e2e/b1.spec.ts"],
            "modify": [],
            "test": []
          },
          "verification": "npx playwright test --grep S2",
          "done_when": "S2 passes",
          "e2e_scenarios": ["S2"]
        }
      ]
    }
  ],
  "e2e": {
    "command": "npx playwright test",
    "runner": "playwright",
    "spec_dir": "e2e/",
    "scenarios": [
      { "id": "S1", "name": "Render markdown" },
      { "id": "S2", "name": "Add comment" }
    ]
  }
}
